const axios = require('axios'); const WEBHOOK_URL = 'https://loot-pay-production.up.railway.app/webhook'; async function main() { console.log('🎯 Testing Production Webhook:', WEBHOOK_URL); const payload = { order_uuid: 'LP-26', amount: 88.10, status: 'Paid', hash: 'test_hash' }; try { const response = await axios.post(WEBHOOK_URL, payload, { headers: { 'Content-Type': 'application/json' }, validateStatus: () => true }); console.log('Response:', response.status, response.data); } catch (error) { console.log('Error:', error.message); } } main();
